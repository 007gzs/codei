[package]
name = "codei-server"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "HTTP server for CodeI — web UI to create sessions and chat with the agent."
keywords = ["codei", "ai-coding", "agent", "server"]

[[bin]]
name = "codei-server"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
tokio = { workspace = true }
codei-sdk = { workspace = true, features = ["server"] }
